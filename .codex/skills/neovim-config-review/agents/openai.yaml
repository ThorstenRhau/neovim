interface:
  display_name: "Neovim Config Review"
  short_description: "Audit this Neovim 0.12 config"
  default_prompt: "Use $neovim-config-review to run a full read-only audit of this Neovim 0.12 configuration, including plugin documentation coverage, and report verified findings only."
